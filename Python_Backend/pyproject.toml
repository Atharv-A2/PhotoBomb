[project]
name = "personal-media-platform"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy>=2",
    "psycopg[binary]",
    "alembic",
    "redis",
    "celery",
    "pydantic-settings",
    "python-dotenv",
    "structlog",
]